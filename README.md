# Fox_Selenium_Test
Code Test - create a selenium project that creates a login for Fox, then examines show tabs and collects the last 4 show titles from each page and outputs them to a csv, using "Duplicate Entry" instead of the title if the show was in a previous tab
